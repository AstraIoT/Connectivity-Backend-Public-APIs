title: CommandResponse
type: object
description: Response to a command sent to a device.
properties:
  command:
    $ref: ./command.v1.yaml
  deviceResponse:
    type: string
    description: The response that the Astra device has had for the previous command.
required:
  - command
  - deviceResponse
